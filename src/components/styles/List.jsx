import styled from '@emotion/styled';

const List = styled.ul({
  fontSize: '2rem',
  display: 'flex',
  justifyContent: 'center',
  flexWrap: 'wrap',
  width: '60vw',
  paddingTop: '30px',
});

export default List;
